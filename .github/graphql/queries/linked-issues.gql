query ($owner: String!, $repo: String!, $pull_request: Int!, $first: Int = 20) {
  repository(owner: $owner, name: $repo) {
    pullRequest(number: $pull_request) {
      closingIssuesReferences(
        first: $first
        orderBy: { direction: ASC, field: CREATED_AT }
      ) {
        edges {
          node {
            number
          }
        }
      }
    }
  }
}
